builds:
  - main: arcd/main.go
    binary: arcd
    goos:
      - windows
      - darwin
      - linux
    goarch:
      - amd64
      - arm
    env:
      - CGO_ENABLED=1
      - CC=arm-linux-gnueabi-gcc

 hooks:
      pre: env
# post: ./script.sh
archive:
  format: tar.gz
  replacements:
    darwin: macOS
